<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/layouts.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/search.css">
	<title>Document</title>
</head>
<body>
	<%- include("layouts/navbar.ejs", { currentPage: "search" }); -%>
	<main>
		<form class="search-bar" action="/search" method="GET">
			<input class="search-input" type="search" name="q" placeholder="Buscar Posts" value="<%= query %>">
			<button class="search-submit" type="submit"><span class="material-symbols-outlined">search</span></button>
		</form>		
		<div class="posts">
			<% if (posts.length == 0) { %>
				<h1>No hay resultados para: <%- query %></h1>
			<% }%>
			<% for (let i = 0; i < posts.length; i++) { %>
                <article class="post">
					<h3><%= posts[i].title %></h3>
					<p><%= posts[i].description %></p>
					<a href="/post/<%= posts[i].title %>">Leer más</a>
				</article>
            <% } %> 
		</div>
	</main>
	<%- include("layouts/footer.ejs", { currentPage: "search" }); -%>
</body>
</html>