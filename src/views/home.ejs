<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/layouts.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/home.css">
    <title>pabloCode010</title>
</head>
<body>
    <%- include("layouts/navbar.ejs", { currentPage: "home" }); -%>
    <header class="about-section section">
        <h1 class="about-title">My-Blog</h1>
        <p class="about-description">Estudiante de Informática apasionado por la tecnología. Comparto recursos en mi blog.</p>
        <p class="welcome-message">¡Bienvenido!</p>
    </header>
    <section class="section latest-videos">
        <form class="search-bar-mobile" action="/search" method="GET">
            <input class="search-input" type="search" name="q" placeholder="Buscar Posts">
            <button class="search-submit" type="submit"><span class="material-symbols-outlined">search</span></button>
        </form>
        <% if (videoLinks.length > 0) { %>
            <h2 class="section-title">últimos videos</h2>
            <div class="videos-scroll">
                <% for (let i = 0; i < videoLinks.length; i++) { %>
                    <article class="video">
                        <img src="<%= videoLinks[i].imageUrls[0] %>" alt="<%= videoLinks[i].imageUrls[1] %>">
                        <a class="video-link" href="<%= videoLinks[i].videoUrl %>" target="_blank">
                            <h3 class="title limit-text"><%= videoLinks[i].title %></h3>
                        </a>
                    </article>
                <% } %> 
            </div>
        <% }%>
        <div class="links">
            <a href="https://www.tiktok.com/@pablocode010" class="link" target="_blank">tiktok</a>
        </div>
    </section>
    <section class="section latest-posts">
        <h2 class="section-title">últimos posts</h2>
        <div class="posts-container">
            <% for (let i = 0; i < latestPosts.length; i++) { %>
            <article class="post">
                <div class="post-summary">
                    <img src="<%= latestPosts[i].imageUrls[0] %>" alt="<%= latestPosts[i].imageUrls[1] %>">
                    <div class="post-text-container">
                        <h3 class="post-title"><%= latestPosts[i].title %></h3>
                        <p class="sypnosis limit-text"><%= latestPosts[i].description %></p>
                    </div>
                </div>
                <a class="post-link" href="/post/<%= latestPosts[i].title %>">Leer más</a>
            </article>
            <% } %>            
        </div>
    </section>
    <% if (recommendedPosts.length > 0) { %>
        <section class="recommended-posts section">
            <h2 class="section-title">recomendaciones</h2>
            <div class="posts-container">
                <% for (let i = 0; i < recommendedPosts.length; i++) { %>
                <article class="post">
                    <div class="post-summary">
                        <img src="<%= recommendedPosts[i].imageUrls[0] %>" alt="<%= recommendedPosts[i].imageUrls[1] %>">
                        <div class="post-text-container">
                            <h3 class="post-title"><%= recommendedPosts[i].title %></h3>
                            <p class="sypnosis limit-text"><%= recommendedPosts[i].description %></p>
                        </div>
                    </div>
                    <a class="post-link" href="/post/<%= recommendedPosts[i].title %>">Leer más</a>
                </article>
                <% } %>       
            </div>
        </section>  
    <% }%>
    <%- include("layouts/footer.ejs", { currentPage: "home" }); -%>
</body>
</html>